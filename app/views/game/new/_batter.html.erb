<table class=<%= batter_class(predicted)%>>
  <caption><%= caption %></caption>
  <thead>
    <tr>
      <th></th>
      <th colspan="8"><%= "#{@season.year} vs #{handedness_header(left)}" %></th>
      <th colspan="8">Last 14 Days</th>
      <% Season.all.order("id DESC")[1..-1].each do |season| %>
      <th colspan="8"><%= "#{season.year} vs #{handedness_header(left)}" %></th>
      <% end %>
      <th colspan="2"><%= "#{@season.year}" %></th>
      <th colspan="2">Last 14 Days</th>
      <% Season.all.order("id DESC")[1..-1].each do |season| %>
        <th colspan="2"><%= "#{season.year}" %></th>
      <% end %>
    </tr>
    <tr>
      <th>Name</th>
      <% (Season.all.size + 1).times do %>
        <th>wOBA</th>
        <th>OPS+</th>
        <th>AB</th>
        <th>SO</th>
        <th>BB</th>
        <th>FB%</th>
        <th>GB%</th>
        <th>LD%</th>
      <% end %>
      <% (Season.all.size + 1).times do %>
        <th>Rank</th>
        <th>AB</th>
      <% end %>
    </tr>
  </thead>
  <tbody class="batter">
    <% batters.each do |batter| %>
      <tr>
        <td class="name"><%= "#{batter.lineup}. #{batter.player.name} (#{batter.player.bathand})"%></td>
        <% batter_stats(batter, left).each do |stat| %>
          <%= render partial: "game/new/batter_stat", locals: { stat: stat } %>
        <% end %>
        <% batter_stats(batter, left).each do |stat| %>
          <td><%= stat.wrc %></td>
          <td><%= stat.ab %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
    <%= render partial: "game/new/sum_batter", locals: { batters: batters, left: handedness(left) } %>
</table>